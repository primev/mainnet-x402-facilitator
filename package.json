{
  "name": "mainnet-x402-facilitator-monorepo",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@8.9.2",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.5.6",
    "typescript": "^5.8.3"
  }
}
